services:
  - type: web
    name: megler-monitor-web
    runtime: docker
    plan: starter
    region: frankfurt
    dockerfilePath: ./Dockerfile.web
    dockerContext: .
    envVars:
      - key: NEXT_PUBLIC_API_BASE
        value: "https://meglermonitor-api.onrender.com"
      - key: NODE_ENV
        value: "production"
      - key: FORCE_REBUILD
        value: "20251029-fix-api-url"

  - type: web
    name: megler-monitor-api
    runtime: docker
    plan: starter
    region: frankfurt
    dockerfilePath: ./MeglerMonitor/infra/Dockerfile.api
    dockerContext: ./MeglerMonitor
    buildCommand: ""
    startCommand: "node api/dist/index.js"
    envVars:
      - key: NODE_ENV
        value: "production"
      - key: DATABASE_URL
        fromDatabase:
          name: megler-monitor-db
          property: connectionString
      - key: USE_SAMPLE
        value: "false"
      - key: PORT
        value: "10000"
      - key: API_PORT
        value: "10000"
      - key: CORS_ORIGIN
        value: "*"

databases:
  - name: megler-monitor-db
    databaseName: megler_monitor
    user: megler_user
    plan: free
    region: frankfurt
